"""PriceWise — a house-price prediction model (classic ML, scikit-learn).

Phase 1 (this package): data loading, feature preprocessing, training with model
selection, and evaluation. Phase 2 will add a serving API on top of the saved
model artifact via `pricewise.predict`.
"""

__version__ = "0.1.0"
