# Runtime-only dependencies for training + serving (used by the Docker image).
# Excludes matplotlib (only needed for evaluate plots) and test tooling.
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
xgboost>=2.0
joblib>=1.3
fastapi>=0.110
uvicorn[standard]>=0.29
